<Window x:Class="WpfAppFactura.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppFactura"
        mc:Ignorable="d"
        Title="Generador de Facturas" Height="326" Width="364" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Generador de Facturas PDF" 
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Fecha del Pedido:" FontWeight="SemiBold"/>
            <DatePicker x:Name="DatePickerFecha" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="ID Cliente:" FontWeight="SemiBold"/>
            <TextBox x:Name="TextBoxIdCliente" Margin="0,5,0,0" Height="25"/>
        </StackPanel>

        <TextBlock Grid.Row="3" x:Name="TextBlockMensaje" 
                   TextWrapping="Wrap" Foreground="Red" 
                   Margin="0,10,0,10"/>

        <Button Grid.Row="5" Content="Generar Factura PDF" 
                HorizontalAlignment="Center" 
                Height="40" Width="180"
                Click="ButtonGenerarFactura_Click"/>
    </Grid>
</Window>